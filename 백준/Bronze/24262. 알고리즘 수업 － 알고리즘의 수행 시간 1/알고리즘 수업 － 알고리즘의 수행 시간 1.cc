#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    int n;
    int cnt = 1, degree = 0;
    cin >> n;

    cout << cnt << "\n";
    cout << degree << "\n";
    
    return 0;
}